@model ClinicalAutomationSystem_MVC_.Models.PatientModel
@{
    ViewBag.Title = "SendMessageTo";
}

<h2>SendMessageTo</h2>

<div class="col-md-4">
    @{ Html.RenderPartial("_PatientMenu");}
</div>
@*<div style="float:left;width:70%;padding-left:40px;">*@
    

    <div class="col-md-8">
        <table class="table table-striped table-responsive" border="1">
            <tr>
                <th>Doctor Name</th>
                <th>Appointment date</th>
                <th></th>
            </tr>
            @foreach (var item in Model.ListApp)
            {
                <tr>
                    @*@if (item.AppointmentStatus == "Accepted")
                        {*@
                    <td>@item.DoctorName</td>
                    <td>@item.AppointDate</td>
                    <td><a href="@Url.Action("Inbox", "Patient", new { id = item.AppointId })">Send Message </a></td>
                    @*}*@
                </tr>
            }
        </table>
    </div>
@*</div>*@


